from distutils.core import setup

setup(name='combine_sorted',
      version='0.1',
      description='Print all unique lines from sorted files in a directory in sorted order',
      author='David Lichtenberg',
      author_email='hello [at] davidlichtenberg [dot] com',
      packages=['combine_sorted'],
     )
